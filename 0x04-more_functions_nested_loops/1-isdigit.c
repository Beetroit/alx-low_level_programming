#include "main.h"

/**
 * _isupper - Checks if input is uppercase or not
 * @c: Input to be checked
 *
 * Return: 0
*/

int _isdigit(int c)
{
	int val = 0;

	if (c >= 48 && c <= 58)
	{
		val = 1;
	}
	return (val);
}
